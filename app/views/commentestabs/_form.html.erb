<%= form_for(@commentestab) do |f| %>
  <% if @commentestab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commentestab.errors.count, "error") %> prohibited this commentestab from being saved:</h2>

      <ul>
      <% @commentestab.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :establishment %><br>
    <%= f.text_field :establishment %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
