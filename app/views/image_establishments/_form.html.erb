<%= form_for(@image_establishment) do |f| %>
  <% if @image_establishment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_establishment.errors.count, "error") %> prohibited this image_establishment from being saved:</h2>

      <ul>
      <% @image_establishment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :establishment %><br>
    <%= collection_select(:image_establishment, :establishment_id, Establishment.all, :id, :reason, :prompt => 'Selecione o Parceiro') %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
